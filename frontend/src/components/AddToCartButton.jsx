import React from "react"

const AddToCartButton = () => {
	// put on all listings
	// when pressed, send info/add information to the database
	// create
	
	return <div>AddToCartButton</div>
}

export default AddToCartButton
